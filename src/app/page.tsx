import MailPilotClient from '@/components/mail-pilot-client';

export default function Home() {
  return (
    <main className="flex h-screen flex-col bg-background">
      <MailPilotClient />
    </main>
  );
}
